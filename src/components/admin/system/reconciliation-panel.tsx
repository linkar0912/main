"use client";
export function ReconciliationPanel({ expiredClaims, onRun }: { expiredClaims: number | null; onRun: (action: "run_delivery_reconciliation" | "run_usage_reconciliation") => void }) { return <article className="panel admin-system-card"><div className="panel-heading"><div><p className="eyebrow">Maintenance</p><h2>Reconciliation</h2></div></div><p className="muted">{expiredClaims === null ? "Expired claim count unavailable." : `${expiredClaims} expired delivery claims detected.`}</p><div className="admin-command-actions"><button className="button button-secondary button-small" onClick={() => onRun("run_delivery_reconciliation")}>Reconcile deliveries</button><button className="button button-secondary button-small" onClick={() => onRun("run_usage_reconciliation")}>Reconcile usage</button></div></article>; }
