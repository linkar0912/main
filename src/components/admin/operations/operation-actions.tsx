"use client";
import { useState } from "react";
import type { AdminOperationDetail } from "@/src/lib/admin/operations/types";
import { ReasonDialog } from "../shared/reason-dialog";
const warning: Record<string, string> = { archive: "This removes the resource from active operation without deleting history.", delete: "This deletion is allowed only when linked history is absent.", retry: "Retry is blocked when a provider receipt already exists.", reprocess: "The original provider event ID is reused and replay is capped.", cancel_pending: "Only work that has not been sent is cancelled." };
export function OperationActions({ detail, onComplete }: { detail: AdminOperationDetail; onComplete: (message: string) => void }) { const [action, setAction] = useState<string | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); async function execute(reason: string) { if (!action) return; setBusy(true); setError(null); try { const response = await fetch(`/api/admin/operations/${detail.kind}/${detail.id}`, { method: "PATCH", headers: { "content-type": "application/json", "x-admin-reason": reason, "idempotency-key": `admin-${crypto.randomUUID()}` }, body: JSON.stringify({ action, version: detail.version, input: {} }) }); const body = await response.json().catch(() => ({})) as { error?: string }; if (!response.ok) throw new Error(body.error ?? "operation_failed"); setAction(null); onComplete(`${action.replaceAll("_", " ")} completed`); } catch (cause) { setError(cause instanceof Error ? cause.message.replaceAll("_", " ") : "Operation failed"); } finally { setBusy(false); } } return <div><h3>Allowed actions</h3>{error ? <div className="form-error" role="alert">{error}</div> : null}<div className="admin-command-actions">{detail.allowedActions.filter((item) => !["update", "update_destination", "restore_version", "export_one"].includes(item)).map((item) => <button className="button button-secondary button-small" type="button" onClick={() => setAction(item)} key={item}>{item.replaceAll("_", " ")}</button>)}</div>{action ? <ReasonDialog title={`${action.replaceAll("_", " ")} ${detail.title}`} warning={warning[action]} busy={busy} onCancel={() => setAction(null)} onConfirm={execute} /> : null}</div>; }
