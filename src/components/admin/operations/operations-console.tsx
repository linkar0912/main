"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useCallback, useEffect, useRef, useState } from "react";
import { Activity, ArrowRight, RefreshCcw } from "lucide-react";
import type { AdminOperationDetail, AdminOperationItem, AdminOperationKind, AdminOperationPage } from "@/src/lib/admin/operations/types";
import { adminOperationKinds } from "@/src/lib/admin/operations/types";
import { CursorTable } from "../shared/cursor-table";
import { FilterBar } from "../shared/filter-bar";
import { OperationDetailDrawer } from "./operation-detail-drawer";

export function OperationsConsole({ kind, page, filters }: { kind: AdminOperationKind; page: AdminOperationPage; filters: Record<string, string> }) { const router = useRouter(); const [selected, setSelected] = useState<AdminOperationDetail | null>(null); const [drawerOpen, setDrawerOpen] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null); const returnFocus = useRef<HTMLButtonElement | null>(null); const navigate = useCallback((next: Record<string, string>) => { const params = new URLSearchParams({ ...filters, ...next }); for (const [key, value] of [...params]) if (!value) params.delete(key); router.push(`/admin/operations?${params}`); }, [filters, router]); async function open(item: AdminOperationItem, button: HTMLButtonElement) { returnFocus.current = button; setDrawerOpen(true); setLoading(true); setError(null); try { const response = await fetch(`/api/admin/operations/${item.kind}/${item.id}`); const body = await response.json() as { data?: AdminOperationDetail; error?: string }; if (!response.ok || !body.data) throw new Error(body.error ?? "operation_unavailable"); setSelected(body.data); } catch (cause) { setError(cause instanceof Error ? cause.message.replaceAll("_", " ") : "Operation unavailable"); } finally { setLoading(false); } } function close() { setDrawerOpen(false); setSelected(null); setTimeout(() => returnFocus.current?.focus(), 0); } useEffect(() => { const timer = window.setInterval(() => { if (document.visibilityState === "visible" && !drawerOpen) router.refresh(); }, 20_000); return () => window.clearInterval(timer); }, [drawerOpen, router]); return <main className="page-wrap admin-resource-page"><header className="page-header"><div><p className="eyebrow">Linkar operator / cross-tenant work</p><h1>Operations</h1><p className="muted page-lede">Inspect and recover application resources without exposing message bodies or provider payloads.</p></div><button className="button button-secondary" type="button" onClick={() => router.refresh()}><RefreshCcw size={16} /> Refresh</button></header><nav className="admin-section-tabs" aria-label="Operation types">{adminOperationKinds.map((item) => <button className={item === kind ? "is-active" : ""} aria-current={item === kind ? "page" : undefined} type="button" key={item} onClick={() => navigate({ kind: item, cursor: "" })}>{item.replaceAll("_", " ")}</button>)}</nav><FilterBar initial={filters} onApply={(next) => navigate({ ...next, cursor: "" })} />{notice ? <div className="form-success" role="status">{notice}</div> : <span className="sr-only" aria-live="polite">{page.items.length} results</span>}<section className="panel admin-table-panel" aria-label={`${kind} operations`}><CursorTable items={page.items} onOpen={open} /></section><nav className="admin-pagination"><span className="muted"><Activity size={14} /> {page.items.length} results on this page</span>{page.nextCursor ? <Link className="button button-secondary" href={`/admin/operations?${new URLSearchParams({ ...filters, kind, cursor: page.nextCursor })}`}>Next page <ArrowRight size={16} /></Link> : <span className="muted">End of results</span>}</nav>{drawerOpen ? <><button className="admin-drawer-scrim" aria-label="Close operation detail" onClick={close} /><OperationDetailDrawer detail={selected} loading={loading} error={error} onClose={close} onComplete={(message) => { setNotice(message); close(); router.refresh(); }} /></> : null}</main>; }
