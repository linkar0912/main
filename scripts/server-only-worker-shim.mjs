// The standalone BullMQ worker is a trusted Node server runtime. Next.js uses
// `server-only` as a compile-time guard, but its default runtime entry throws
// when bundled outside the React Server Components condition.
export {};
